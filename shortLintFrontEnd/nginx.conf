server {
    listen 80;
    server_name _;
    root /usr/share/nginx/html;
    index index.html;

    # react-router client-side routes must all fall back to index.html.
    location / {
        try_files $uri $uri/ /index.html;
    }
}